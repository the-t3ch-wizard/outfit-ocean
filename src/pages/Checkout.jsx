import React from 'react'

export default function Checkout() {

  // get products saved to cart as small product card and their cost detail then, add button to confirm,
  // then form to fill address,
  // then select payment method and then confirm
  // order collection will store the orders as customerId, sellerId, quantity, price, Id.unique

  return (
    <div className=' w-full min-h-screen'>
      Checkout
    </div>
  )
}
